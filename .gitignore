.DS_Store
.env
*.env
*.pem
*.key
# Exceptions: checked-in test fixtures, public certificates only -- never a private key.
# ADR-S (crates/vg-vault/src/certificate.rs's own test suite, and the vendored
# veil-custodian cross-repo fixture) needs these present for anyone who clones the repo.
!crates/vg-core/tests/fixtures/custodian/*.pem
!crates/vg-vault/tests/fixtures/*.pem
*.log
__pycache__/
node_modules/
.venv/
dist/
build/
.project-setup/

# Rust (Task T01)
/target/
**/target/

# Internal Hekton factory state, untracked from this PUBLIC repo (2026-07-19 doc-safety
# audit). This handles ONLY these two dirs. Private-identity prose de-linkage across the
# docs was completed in the 2026-07-19 writer pass; machine-path relativisation in the
# context files remains a separate pending pass. NONE of this purges git HISTORY — the
# content remains in prior commits until a deliberate history remediation (see the Repo
# Sanitisation workflow). Not "closed".
# - .control-tower/: orchestrator run transcripts/ledgers
# - mind-palace/: repo-local Obsidian vault mirror (belongs to the vault-sync flow, not the
#   public product repo). Both kept locally, synced via scripts.
.control-tower/
mind-palace/

# --- Hekton control plane (boundary: never ship in a public repo) ---
# Untracked 2026-07-31 after the secret-sauce audit found these published.
# Files remain on disk; local tooling (architecture map, session logging) is unaffected.
.hekton/
mind-palace/
docs/session-log.md
docs/local-assumptions.md
docs/depth-decision.md
docs/human-understanding-check.md
docs/blog-signals.md
docs/gremlin-radar.md
scripts/sync-mirror-to-vault.sh
scripts/check-mirror-drift.sh
scripts/setup-hooks.sh

# Agent config: describes the Hekton control plane and machine layout, not the product.
AGENTS.md
CLAUDE.md
CODEX.md
.env.example

# Local-only scriptorium-lab context-pilot corpus (2026-09-16): a hand-authored knowledge
# mirror of this repo's own decisions/risks, read by `scriptorium context` for task-scoped
# agent context during dev sessions. Same boundary as AGENTS.md/CLAUDE.md above -- describes
# internal factory tooling, not the product, never ships in the public repo.
knowledge/

# >>> hekton-boundary (secret-sauce audit 2026-07-31) >>>
# Control plane, vault mirror and agent config: never ship in a public repo.
# Files stay on disk; only the published tree changes.
.hekton/
mind-palace/
docs/session-log.md
docs/local-assumptions.md
docs/depth-decision.md
docs/human-understanding-check.md
docs/blog-signals.md
docs/gremlin-radar.md
docs/walkthroughs/
scripts/sync-mirror-to-vault.sh
scripts/check-mirror-drift.sh
scripts/setup-hooks.sh
AGENTS.md
CLAUDE.md
CODEX.md
knowledge/
# <<< hekton-boundary <<<

# Track H, H1 fixture governance (fork F15, 2026-09-14): raw Codex-spike captures may
# contain real upstream account/session-shaped values (see scripts/h1-fixtures/scrub.py's
# own doc comment). Never committed raw — only scripts/h1-fixtures/sanitized/*.jsonl,
# produced by running scrub.py over a raw capture, is ever meant to reach git.
scripts/h1-fixtures/raw/

# --- Hekton open-core boundary (retrofit) ---
# Factory-internal artefacts. Untracked 2026-09-19; they remain in the
# working tree and in this repo's history.
# Corrected 2026-09-19: no private-sibling repo exists for this project --
# the original comment's claim was false, found by adversarial review.
docs/retire-promote-review.md
